#include <iostream>
using namespace std;
int main();
int g(int a, int b);
cout<<"enter int a and int b"<<"g"<<endl;
cin>>"a,b";
if (a>b)
{
	int c;
	a/b == cxb + a%b
	a%b=r
	if (r==0)
	{
		cout<<"c is the g"<<endl;
	}
	else if (r>0)
	{
		int c;
		b/r == c*r + b%r
		if (b%r==0)
		{
			cout<<"c is the g"<<endl;
		}
		else
		{
			return a,b;
		}
	}
	return 0;
}
